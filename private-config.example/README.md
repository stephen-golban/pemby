# private-config.example

Placeholder copy of the private config layout, for local development and CI only.
Nothing here is real: prompts say PLACEHOLDER, weights are equal, source lists are empty.
`manifest.json` has `"placeholder": true`, so staging and production refuse to load it.

The real files live in the private repo `stephen-golban/pemby-private`.
See `docs/private-config.md`.
