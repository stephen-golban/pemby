// The tracker's own pieces (PLAN D9). Operate density on the landing's world: hairline-ruled rows,
// outline-pill secondary actions, no boxes around cells, no tilt. The row grammar is the profile
// ledger's, imported from `app/profile/_shared/fields.tsx` rather than rebuilt.
export { TrackerColumn } from "./tracker-column";
export { TrackerRow, type TrackerRowActions } from "./tracker-row";
